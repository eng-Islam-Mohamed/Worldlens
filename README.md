# WorldLens

## Environment

Create a local `.env.local` file with:

```env
DEEPSEEK_API_KEY=your_deepseek_api_key_here
```

For production, add the same `DEEPSEEK_API_KEY` variable in your hosting provider's server environment settings.
